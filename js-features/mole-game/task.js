for (i = 1; i < 10; i++) {
    let `holeElement${i}` = document.getElementById(`hole${i}`)
    `holeElement${i}`.onclick = () => checkMole(`holeElement${i}`))

}

function checkMole(holeElement) {
    
}